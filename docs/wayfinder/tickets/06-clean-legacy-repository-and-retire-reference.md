# 06 - Clean the retired product model from the repository

**What to build:** Once Profile-owned Contract access and Contract-only authoring are established, obsolete product code, routes, tests, assets, documentation, and generated output are removed before any new MilestonePay-informed UX is delivered.

**Blocked by:** 01 - Establish Profile-owned Contract access; 02 - Retire legacy Contract authoring and routes.

**Status:** complete - verified 2026-08-10

- [x] Repository-wide checks confirm no runnable product code, route, asset, test, documentation, or generated output depends on the retired product model or non-Contract terminology.
- [x] Production build, type checking, automated tests, and repository integrity checks pass after cleanup.

## Evidence - 2026-08-10

- Removed retired Workspace routes, loaders, bundle entry, browser assets, unused dashboard script, legacy product documents, and generated build artifacts. Browser build output is now ignored and regenerated by the production build.
- Renamed the Vault's Contract-facing factory, state, version-hash, and EIP-712 acceptance vocabulary; the Solidity scenario suite exercises the migrated ABI.
- The authenticated route test proves retired routes and assets (`/workspace`, `/workspace-list.html`, `/workspace.js`, `/workspace.css`, `/workspace.bundle.js`, `/contacts.html`, and `/api/workspaces`) return 404.
- Repository scans found no retired terms in active server, build, or contract source; all remaining mentions are scoped to migration specifications, tracker history, negative regression checks, or vocabulary exclusions.
- `npm.cmd --prefix web run build`, `npm.cmd --prefix web run typecheck`, `npm.cmd --prefix web run contracts:check`, `npm.cmd --prefix web test` (59 passing), and `git diff --check` pass.

## Remaining gaps

None for this ticket's acceptance criteria.
