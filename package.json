{
  "name": "pactflow",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "start": "concurrently --kill-others-on-fail --names frontend,backend --prefix-colors cyan,green \"npm run start --workspace frontend\" \"npm run start --workspace backend\"",
    "build": "npm run build --workspace frontend",
    "test": "npm run test --workspace backend",
    "typecheck": "npm run typecheck --workspace frontend && npm run typecheck --workspace backend"
  },
  "devDependencies": { "concurrently": "^9.2.1" },
  "engines": { "node": ">=20" }
}
